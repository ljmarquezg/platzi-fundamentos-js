/* 
for (variable; condicion; incremento {
    // Código a ejecutar
}) */

const list = ["arroz", "papa", "carne", "pollo", "pescado"];

for (let i = 0; i < list.length; i++) {
    console.log(list[i]);
}